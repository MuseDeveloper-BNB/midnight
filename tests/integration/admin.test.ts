describe('Admin integration', () => {
  it('user role change works', async () => {
    expect(true).toBe(true);
  });

  it('user deactivation works', async () => {
    expect(true).toBe(true);
  });

  it('moderation history query works', async () => {
    expect(true).toBe(true);
  });
});
